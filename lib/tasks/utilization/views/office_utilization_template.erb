Per Office Utilization Report
-----------------------------
* Utilization is calculated as Billing FTEs / Assignable FTEs

<% snapshots.each do |snapshot| -%>
<%= "#{snapshot.office.name} - #{snapshot.billing.to_fte} / #{snapshot.assignable.to_fte} = #{snapshot.utilization}% (~ #{snapshot.billable.size} billable people)" %>
<% end %>

