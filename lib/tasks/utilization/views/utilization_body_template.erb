<% snapshots.each do |snapshot| %>
Non Billable - <%= snapshot.non_billable.size %> people
--------------------------------------------
* Percentage shown represents fraction of overhead

<%= person_rows(snapshot.non_billable) %>


Unassignable - <%= snapshot.unassignable.size %> people
--------------------------------------------
* Percentage shown represents fraction of billable time allocated to vacation, etc.

<%= person_rows(snapshot.unassignable) %>


Non Billing - <%= snapshot.non_billing.size %> people
--------------------------------------------
* Percentage shown represents fraction of billable time allocated to projects, but not actually billing

<%= person_rows(snapshot.non_billing) %>


Billing - <%= snapshot.billing.size %> people
--------------------------------------------
* Percentage shown represents fraction of billable time allocated to projects

<%= person_rows(snapshot.billing) %>


Overallocated - <%= snapshot.overallocated.size %> people
--------------------------------------------
* Percentage shown represents additional capacity above billable capacity allocated to projects

<%= person_rows(snapshot.overallocated, false) %>

<% end %>
