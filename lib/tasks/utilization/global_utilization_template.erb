
Global Utilization Report
-------------------------
Global utilization for today is <%= @snapshot.utilization %>%.
  - Currently there are <%= staff_count %> staff members of which <%= fully_billable_count %> are 100% billable, <%= pure_overhead_count %> are 0% billable, and <%= partially_billable_count %> are partially billable.
    - Based on billable time, these <%= staff_count %> staff members equate to <%= fte_billable_count %> FTEs of which <%= assignable_count %> are assignable and <%= unassignable_count %> are not.
    - Of the <%= assignable_count %> assignable FTEs, <%= billing_count %> are billing today.
  - Daily utilization is calculated as billing over assignable
    - For today, that is <%= billing_count %> / <%= assignable_count %> = <%= @snapshot.utilization %>%

